#include <iostream>
#include <stdio.h>
#include <fstream>
#include <string>

using namespace std;

int main()
{
    ifstream infile("test.txt");
    int a;
    cin>>a;
for (int i=0;i<a;i++)
{
  if (infile.good())
  {
    string sLine;
    getline(infile, sLine);
    cout << sLine << endl;
  }
}
  infile.close();
    return 0;
}
